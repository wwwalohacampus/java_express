

*Object 클래스
	: java.lang 에 속한 클래스
	
	- 최상위 클래스
	- 모든 클래스에 강제로 상속된다.
	- 모든 클래스에서 상속받아 사용할 공통 기능이 구현되어 있다.
	
	▶주요 메소드
	- equals(Object obj)		: obj 가 가리키는 객체와 현재 객체를 비교하여 같으면 true 를 리턴
	- getClass()				: 현 객체의 런타임 클래스를 리턴
	- hashCode()				: 현 객체의 해시 코드 값 리턴				
	- toString()				: 현 객체에 대한 문자열 표현을 리턴
	- notify()					: 현 객체에 대해 대기하고 있는 하나의 스레드를 깨운다.
	- notifyAll()				: 현 객체에 대기하는 있는 모든 스레드 깨운다.
	- wait()					: 다른 스레드가 깨울 때까지 현재 스레드를 대기하게 한다.
	